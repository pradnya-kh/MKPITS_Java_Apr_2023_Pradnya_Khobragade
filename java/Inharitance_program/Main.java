import interfacepackage.*;
import interfacepackage.Circle;
import interfacepackage.Rectangle;

public class Main {
    public static void main(String[] args) {
        Circle circle=new Circle(5);
        Rectangle rectangle=new Rectangle(5,2);
            circle.area();
            rectangle.area();
    }
}